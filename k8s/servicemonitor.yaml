apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: matrix-app
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: matrix-app
  endpoints:
    - port: http
      path: /metrics
      interval: 5s