apiVersion: v1
kind: Service
metadata:
  name: matrix-app-service
  labels:
    app: matrix-app
spec:
  type: NodePort
  selector:
    app: matrix-app
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30007